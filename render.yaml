services:
  - type: web
    name: cobalt-due-diligence
    env: docker
    dockerfilePath: ./Dockerfile
    plan: starter
    region: oregon
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
      - key: PORT
        value: "10000"
